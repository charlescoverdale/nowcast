% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_transform.R
\name{nc_aggregate}
\alias{nc_aggregate}
\title{Temporal Aggregation}
\usage{
nc_aggregate(data, to = c("quarterly", "annual"), fun = mean)
}
\arguments{
\item{data}{A data frame with columns \code{date} (Date) and \code{value} (numeric).}

\item{to}{Character. Target frequency: \code{"quarterly"} or \code{"annual"}.}

\item{fun}{Function used for aggregation (default \link{mean}).}
}
\value{
A data frame with columns \code{date} and \code{value} at the target frequency.
}
\description{
Aggregate a time series from a higher frequency to a lower frequency.
For example, convert monthly data to quarterly by taking the mean, sum,
or last observation.
}
\details{
The default aggregation function is \link{mean}, which is appropriate for
\strong{flow variables} measured as rates or indices (e.g. GDP growth, CPI).
For flow variables measured in levels (e.g. total retail sales), use
\code{fun = sum}. For \strong{stock variables} (e.g. interest rates, exchange
rates), use \code{fun = function(x, ...) tail(x, 1)} to take the
end-of-period value.
}
\examples{
monthly <- data.frame(
  date = seq(as.Date("2020-01-01"), as.Date("2020-12-01"), by = "month"),
  value = rnorm(12)
)
nc_aggregate(monthly, to = "quarterly")
nc_aggregate(monthly, to = "annual", fun = sum)
}
