% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nc_transform.R
\name{nc_transform}
\alias{nc_transform}
\title{Stationarity Transformations}
\usage{
nc_transform(data, method = c("diff", "log_diff", "standardize"))
}
\arguments{
\item{data}{A data frame with columns \code{date} and \code{value}, or a numeric
vector.}

\item{method}{Character. One of \code{"diff"} (first difference), \code{"log_diff"}
(log first difference, i.e. percentage growth rate), or \code{"standardize"}
(zero mean, unit variance).}
}
\value{
A data frame with columns \code{date} and \code{value} (if input was a data
frame), or a numeric vector (if input was numeric). The output is shorter
by one observation for \code{"diff"} and \code{"log_diff"}.
}
\description{
Apply common transformations to make a time series stationary. Supports
first differencing, log differencing (growth rates), and standardisation.
}
\examples{
df <- data.frame(
  date = seq(as.Date("2020-01-01"), as.Date("2020-06-01"), by = "month"),
  value = c(100, 102, 101, 105, 103, 108)
)
nc_transform(df, method = "diff")
nc_transform(df, method = "log_diff")
nc_transform(df, method = "standardize")
}
